[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "A FastMango project."
dependencies = [
    "fastmango",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
